# <pep8 compliant>
# Third party packages bundled with this add-on.
#
# pyBIG (https://github.com/ClementJ18/pyBIG), MIT licensed, see pyBIG/LICENSE.
# It is a submodule rather than a pip dependency because a Blender add-on is
# installed from an archive, with no network install step to resolve requirements.
# The repository has no release tags, so the submodule is pinned to the commit
# holding v0.6.6; note that the package lives in a 'pyBIG' directory inside it.
